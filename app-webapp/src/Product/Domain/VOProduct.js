class Product {
  constructor(id, productName, productType, productImage) {
    this.id = id;
    this.productName = productName;
    this.productImage = productImage;
    this.productType = productType;
  }
}

export default Product;
